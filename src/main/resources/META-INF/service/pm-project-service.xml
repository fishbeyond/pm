<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-http="http://www.springframework.org/schema/integration/http"
       xmlns:int-jdbc="http://www.springframework.org/schema/integration/jdbc"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                  http://www.springframework.org/schema/beans/spring-beans.xsd
                  http://www.springframework.org/schema/integration
                  http://www.springframework.org/schema/integration/spring-integration.xsd
                  http://www.springframework.org/schema/integration/http
                  http://www.springframework.org/schema/integration/http/spring-integration-http.xsd
                  http://www.springframework.org/schema/integration/jdbc
                  http://www.springframework.org/schema/integration/jdbc/spring-integration-jdbc.xsd http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd">

    <int:chain input-channel="projectService.findProjectByUserId">
        <int:service-activator expression="@projectService.findProjectByUserId(payload[userId])"/>
    </int:chain>
    <int:chain input-channel="projectService.createProject">
        <int:map-to-object-transformer type="com.hs.pm.server.project.dao.Project"/>
        <int:service-activator expression="@projectService.createProject(payload)"/>
    </int:chain>
    <int:chain input-channel="projectService.modifyProject">
        <int:map-to-object-transformer type="com.hs.pm.server.project.dao.Project"/>
        <int:service-activator expression="@projectService.modifyProject(payload)"/>
    </int:chain>
    <int:chain input-channel="projectService.deleteProject">
        <int:service-activator expression="@projectService.deleteProject(payload[projectId])"/>
    </int:chain>

    <int:chain input-channel="projectService.addMemberFromFriend">
        <int:service-activator expression="@projectService.addMemberFromFriend(payload[projectId],payload[userIdList])"/>
    </int:chain>
    <int:chain input-channel="projectService.addMemberFromLinkMan">
        <int:service-activator expression="@projectService.addMemberFromLinkMan(payload[projectId],payload[phoneNoList])"/>
    </int:chain>
</beans>