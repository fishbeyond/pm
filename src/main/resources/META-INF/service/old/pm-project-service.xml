<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-http="http://www.springframework.org/schema/integration/http"
       xmlns:int-jdbc="http://www.springframework.org/schema/integration/jdbc"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                  http://www.springframework.org/schema/beans/spring-beans.xsd
                  http://www.springframework.org/schema/integration
                  http://www.springframework.org/schema/integration/spring-integration.xsd
                  http://www.springframework.org/schema/integration/http
                  http://www.springframework.org/schema/integration/http/spring-integration-http.xsd
                  http://www.springframework.org/schema/integration/jdbc
                  http://www.springframework.org/schema/integration/jdbc/spring-integration-jdbc.xsd http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd">

    <int:chain input-channel="projectAction.findProjectByUserId">
        <int:service-activator expression="@projectAction.findProjectByUserId(payload[userId])"/>
    </int:chain>
    <int:chain input-channel="projectAction.createProject">
        <int:map-to-object-transformer type="com.hs.whocan.component.project.dao.Project"/>
        <int:service-activator expression="@projectAction.createProject(payload)"/>
    </int:chain>
    <int:chain input-channel="projectAction.modifyProject">
        <int:map-to-object-transformer type="com.hs.whocan.component.project.dao.Project"/>
        <int:service-activator expression="@projectAction.modifyProject(payload)"/>
    </int:chain>
    <int:chain input-channel="projectAction.deleteProject">
        <int:service-activator expression="@projectAction.deleteProject(payload[projectId])"/>
    </int:chain>

    <int:chain input-channel="projectAction.addMemberFromFriend">
        <int:service-activator expression="@projectAction.addMemberFromFriend(payload[projectId],payload[userIdList])"/>
    </int:chain>
    <int:chain input-channel="projectAction.addMemberFromLinkMan">
        <int:service-activator expression="@projectAction.addMemberFromLinkMan(payload[projectId],payload[phoneNoList])"/>
    </int:chain>
</beans>